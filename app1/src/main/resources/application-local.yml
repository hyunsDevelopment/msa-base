spring:
  application:
    name: app1
  config:
    import:
      - classpath:core-local.yml
      - classpath:config/mybatis/mybatis.yml
      - classpath:config/db/database-local.yml
      - classpath:config/logback/logback-local.yml
server:
  port: 10010
springdoc:
  swagger-ui:
    path: /
